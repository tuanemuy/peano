table:
  name: users
  schema: public
object_relationships:
  - name: profile
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_profiles
          schema: public
